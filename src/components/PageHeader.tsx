export default function PageHeader() {
  return (
   <header className="w-full h-[50px] sm:h-[65px] border-2 border-gray-300 bg-white shadow-header flex items-center justify-center px-2">
      <span className="font-bold text-xl sm:text-2xl text-[#0ab463] leading-none text-center w-full">
        SWStarter
      </span>
    </header>
  );
}
